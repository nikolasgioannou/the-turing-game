# Public asset review

The bundled `public/fonts/press-start.ttf` has its SIL Open Font License in `public/fonts/OFL.txt`; retain that notice when redistributing the font. The social card and favicons are rendered from the project's shared pixel icons and bundled font by `scripts/social-assets.tsx`.

The repository does not currently document the author, source or redistribution permission for `public/music.mp3`. The file's introduction in history is not license evidence. Owner confirmation or a replacement with documented permission is needed before closing ticket f821f8. This review does not change the existing music or assert that it is unlicensed.

Reviewed the committed simulator samples, UI review states and JSON behavior/filtering/similarity fixtures. They contain short game examples and test context, including first names and device/time hints. A targeted scan found no email addresses, phone-number records or API-key patterns in those fixtures; this is not proof of consent or an exhaustive history audit. The behavior fixtures are generated by `scripts/fixtures.ts`, but that alone does not establish the provenance of every seed example. Confirmation of sample provenance remains with the owners. Do not add private source transcripts to fixtures; use fictional examples for new tests.

No sensitive material requiring a history rewrite was established by this review. No history was rewritten or licensing claim inferred from public availability. Project licensing remains separate from asset-specific permissions.
