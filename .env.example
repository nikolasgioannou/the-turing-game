PORT=3000
APP_ORIGIN=http://localhost:3000
DATABASE_URL=
PGLITE_PATH=./data/local

# Required. Never expose this key in client code.
OPENROUTER_API_KEY=
# The bot is pinned to anthropic/claude-haiku-4.5, routed to Anthropic.

# Operator simulator at /sim (Cmd/Ctrl+Shift+= on the site). Unset to disable it entirely.
SIM_KEY=
SIM_LANES=5
