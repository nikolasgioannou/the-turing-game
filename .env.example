PORT=3000
APP_ORIGIN=http://localhost:3000
DATABASE_URL=
PGLITE_PATH=./data/postgres
DAILY_INPUT_TOKEN_CAP=1000000
DAILY_OUTPUT_TOKEN_CAP=100000

# Required. Never expose this key in client code.
OPENROUTER_API_KEY=
# The bot is pinned to anthropic/claude-haiku-4.5, routed to Anthropic.
# Optional path to an existing Python 3.9+ runtime (no Python dependencies needed).
# BOT_PYTHON=/path/to/python3
